{
  "type": "cli",
  "label": "GPT-5.6 Sol",
  "cli": {
    "driver": "codex",
    "executor": "command",
    "command": {
      "binary": "codex",
      "args": [],
      "model": "gpt-5.6-sol",
      "env": {
        "CODEX_HOME": "${HOME}/.codex-aip"
      }
    },
    "runtime": {
      "prompt_delivery": "stdin",
      "managed_args": ["exec"],
      "result_contract": "required"
    }
  }
}
