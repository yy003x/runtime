// Package application groups Runtime use-case orchestration, activation, tool
// assembly, and the process composition root. A CLI Runtime needs explicit
// startup and lifecycle workflows even when no long-lived HTTP server exists.
package application
